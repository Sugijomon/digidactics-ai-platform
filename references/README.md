# References

This folder contains source material, historical exports, research inputs, and implementation references.

Reference files are read-only context. Active product decisions and specifications must be captured in `docs/`.

Suggested subfolders:

- `lovable/` — Lovable exports and generated code snapshots, reference only.
- `routeai/` — RouteAI architecture and source documents.
- `source-docs/` — external or migrated source documents.

